# Governance Agent

This directory will contain the Governance Agent application.

## Structure
- `/react_ui`: React application using ADK.
- `/adk_config`: Configuration files for the ADK agent.

## Setup
1. Initialize React app: `npx create-react-app react_ui`
2. Install ADK dependencies (TBD)
3. Configure ADK in `/adk_config`
